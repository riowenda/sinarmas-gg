interface PrivacyScreenProps {
    text: string,
    handleOnPress?: () => void
}

const PrivacyScreen: React.FC<PrivacyScreenProps> = ({ text, handleOnPress}) => {
    return (
        <>

        </>
    );
};

export default PrivacyScreen;